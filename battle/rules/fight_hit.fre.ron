#![enable(implicit_some)]
// Fight Hit Handler — plays slice sound when player confirms attack
// 攻击命中处理 — 玩家确认攻击时播放切割音效

(
    scope: View,

    rules: [
        (event: Event("fight:hit"),
         actions: [PlaySound("slice")]),
    ],
)
